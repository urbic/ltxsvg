language: perl

perl:
  - "5.18"

sudo: required

dist: trusty

#before_install:
#  - sudo apt-get -qq update
#  - sudo apt-get install -y texlive-extra-utils

addons:
  apt:
    packages:
      - libio-captureoutput-perl
      - libdigest-md5-perl 
      - texlive-latex-base
      - texlive-fonts-extra
      - texlive-binaries
      - texlive-extra-utils
